[project]
name = "iterdot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
  "mypy>=1.11.1",
  "ruff>=0.5.6",
  "basedpyright>=1.15.1",
  "pytest>=8.3.3",
  "pyright>=1.1.383",
]

[tool.basedpyright]
venvPath = "."
pythonVersion = "3.13"
